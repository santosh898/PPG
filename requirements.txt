# Python dependencies for the Vercel serverless function (api/index.py).
# Kept in sync with backend/requirements.txt, minus uvicorn (Vercel provides the
# ASGI server). Set MONGODB_URI and GEMINI_API_KEY as Vercel environment variables.
fastapi>=0.115
pymongo[srv]>=4.9
pydantic>=2.7
pydantic-settings>=2.4
python-dotenv>=1.0
httpx>=0.27
langchain>=1.0,<2
langgraph>=1.0,<2
langchain-google-genai>=2.0
google-genai>=1.0
